package Test;

import Cards.CardColor;
import Cards.CardValue;
import Cards.UnoCards;

public class UniTestUnoCards extends UnoCards {
    public UniTestUnoCards(CardColor color, CardValue value) {
        super(color, value);
    }
}
